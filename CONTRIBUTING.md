# Contributing

1. Create a focused branch.
2. Keep components in `src/components` and pages in `src/pages`.
3. Keep credentials in `.env`, never source files.
4. Run `npm run build` and `node --check server/index.js`.
5. Update relevant documentation and screenshots.

Use conventional commit messages such as `feat:`, `fix:`, and `docs:`.
