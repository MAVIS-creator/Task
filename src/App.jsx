import { Navigate, Route, Routes } from 'react-router-dom';import { useEffect,useState } from 'react';import Shell from './components/Shell';import LandingPage from './pages/LandingPage';import AuthPage from './pages/AuthPage';import OverviewPage from './pages/OverviewPage';import RepositoryPage from './pages/RepositoryPage';import BrainPage from './pages/BrainPage';import AssistantPage from './pages/AssistantPage';import SettingsPage from './pages/SettingsPage';import TasksPage from './pages/TasksPage';import DocumentsPage from './pages/DocumentsPage';
const getStoredUser=()=>localStorage.getItem('devpal-ai-user')||localStorage.getItem('project-brain-user');const authenticated=()=>Boolean(getStoredUser());function Protected({children}){return authenticated()?children:<Navigate to="/login" replace/>}function App(){const [user,setUser]=useState(authenticated());useEffect(()=>{const sync=()=>setUser(authenticated());window.addEventListener('storage',sync);return()=>window.removeEventListener('storage',sync)},[]);return <Routes><Route path="/" element={user?<Navigate to="/overview" replace/>:<LandingPage/>}/><Route path="/login" element={<AuthPage mode="login" onAuth={()=>setUser(true)}/>}/><Route path="/signup" element={<AuthPage mode="signup" onAuth={()=>setUser(true)}/>}/><Route element={<Protected><Shell/></Protected>}><Route path="/overview" element={<OverviewPage/>}/><Route path="/repository" element={<RepositoryPage/>}/><Route path="/brain" element={<BrainPage/>}/><Route path="/assistant" element={<AssistantPage/>}/><Route path="/tasks" element={<TasksPage/>}/><Route path="/documents" element={<DocumentsPage/>}/><Route path="/settings" element={<SettingsPage/>}/></Route><Route path="*" element={<Navigate to="/" replace/>}/></Routes>}export default App;
