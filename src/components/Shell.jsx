import { Link, useLocation } from 'react-router-dom'; import { Bot, BrainCircuit, FileText, FolderGit2, Github, LayoutDashboard, Settings, Sparkles, ListTodo } from 'lucide-react';
const nav=[['/','Overview',LayoutDashboard],['/repository','Repository',FolderGit2],['/brain','Project Brain',BrainCircuit],['/assistant','AI Assistant',Bot],['/tasks','Tasks',ListTodo],['/documents','Documents',FileText],['/settings','Settings',Settings]];
export default function Shell({children}){const {pathname}=useLocation();const title=nav.find(([to])=>to===pathname)?.[1]||'Project Brain';return <div className="app"><aside><Link className="brand" to="/"><span><BrainCircuit size={22}/></span><b>Project Brain</b></Link><p className="eyebrow">PROJECT INTELLIGENCE</p><nav>{nav.map(([to,label,Icon])=><Link className={pathname===to?'active':''} to={to} key={to}><Icon size={18}/>{label}</Link>)}</nav><div className="sidebar-note"><Sparkles size={16}/><span>Memory engine<br/>ready for sources</span></div></aside><main><header><div><p className="eyebrow">CONNECTED WORKSPACE</p><h1>{title}</h1></div><Link className="settings-link" to="/settings"><Github size={17}/> GitHub connection</Link></header><section className="content">{children}</section></main></div>}
