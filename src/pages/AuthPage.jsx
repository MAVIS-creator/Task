import { Link,useNavigate } from 'react-router-dom';import { BrainCircuit } from 'lucide-react';import { useState } from 'react';
export default function AuthPage({mode,onAuth}){const navigate=useNavigate(),[name,setName]=useState(''),[email,setEmail]=useState('');const signup=mode==='signup';function submit(e){e.preventDefault();localStorage.setItem('project-brain-user',JSON.stringify({name:name||email.split('@')[0],email}));onAuth();navigate('/overview')}return <main className="auth"><Link className="brand" to="/"><span><BrainCircuit size={22}/></span><b>Project Brain</b></Link><form onSubmit={submit}><p className="eyebrow">{signup?'CREATE YOUR WORKSPACE':'WELCOME BACK'}</p><h1>{signup?'Start with your project context.':'Sign in to Project Brain.'}</h1><p className="muted">{signup?'Create an account to connect your first repository.':'Enter your details to continue to your workspace.'}</p>{signup&&<label>Name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" required/></label>}<label>Email<input value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" type="email" required/></label><label>Password<input placeholder="••••••••" type="password" required minLength="6"/></label><button className="button">{signup?'Create account':'Sign in'}</button><p className="switch">{signup?'Already have an account?':'New to Project Brain?'} <Link to={signup?'/login':'/signup'}>{signup?'Sign in':'Create one'}</Link></p></form></main>}
