import { Link,useNavigate,useSearchParams } from 'react-router-dom';import { BrainCircuit,Github } from 'lucide-react';import { useEffect,useState } from 'react';
export default function AuthPage({mode,onAuth}){const navigate=useNavigate(),[query]=useSearchParams(),[name,setName]=useState(''),[email,setEmail]=useState(''),[error,setError]=useState('');const signup=mode==='signup';useEffect(()=>{const session=query.get('session');if(query.get('github')==='failed')setError('GitHub sign-in did not finish. Please try again.');if(session)fetch(`/api/auth/session?session=${encodeURIComponent(session)}`).then(r=>r.json()).then(d=>{if(!d.user)throw new Error();localStorage.setItem('project-brain-user',JSON.stringify(d.user));onAuth();navigate('/overview')}).catch(()=>setError('Your GitHub session expired. Please sign in again.'))},[query,navigate,onAuth]);function submit(e){e.preventDefault();localStorage.setItem('project-brain-user',JSON.stringify({name:name||email.split('@')[0],email}));onAuth();navigate('/overview')}return <main className="auth"><Link className="brand" to="/"><span><BrainCircuit size={22}/></span><b>Project Brain</b></Link><form onSubmit={submit}><p className="eyebrow">{signup?'CREATE YOUR WORKSPACE':'WELCOME BACK'}</p><h1>{signup?'Start with your project context.':'Sign in to Project Brain.'}</h1><p className="muted">{signup?'Create an account to connect your first repository.':'Continue with GitHub or use a local demo account.'}</p><a className="github-login" href="/api/github/connect"><Github size={18}/> Continue with GitHub</a><div className="auth-divider"><span>or</span></div>{signup&&<label>Name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" required/></label>}<label>Email<input value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" type="email" required/></label><label>Password<input placeholder="••••••••" type="password" required minLength="6"/></label><button className="button">{signup?'Create account':'Sign in'}</button>{error&&<p className="error">{error}</p>}<p className="switch">{signup?'Already have an account?':'New to Project Brain?'} <Link to={signup?'/login':'/signup'}>{signup?'Sign in':'Create one'}</Link></p></form></main>}
