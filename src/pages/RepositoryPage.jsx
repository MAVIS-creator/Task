import { Link } from 'react-router-dom'; import { Github, GitBranch } from 'lucide-react'; import Card from '../components/Card'; const events=['feat: connect project workspace','docs: map architecture decisions','fix: stabilize repository analysis'];
export default function RepositoryPage(){return <><div className="page-heading"><div><p className="eyebrow">REPOSITORY INTELLIGENCE</p><h2>Source-aware project context.</h2><p>Repository analysis becomes available after you select a GitHub repository in Settings.</p></div><Link className="button" to="/settings"><Github size={17}/> Connect repository</Link></div><div className="grid"><Card className="wide"><p className="eyebrow">AVAILABLE FROM SOCIALITY</p><h3>GitHub activity feed</h3><div className="empty-list">{events.map(event=><div key={event}><GitBranch size={16}/><code>{event}</code><span>Waiting for your repository</span></div>)}</div></Card><Card><p className="eyebrow">ANALYSIS OUTPUT</p><h3>What will be mapped</h3><ul><li>Repository structure and stack</li><li>Commits, PRs, and issues</li><li>Development timeline</li><li>Activity summaries</li></ul></Card></div></>}
