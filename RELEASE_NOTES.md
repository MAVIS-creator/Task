# Release Notes — Hackathon MVP

Project Brain demonstrates a memory-centric developer workspace. The release includes a polished public flow, GitHub OAuth sign-in, task continuity UI, AI document drafting, and platform-ready deployment configuration.

Known limitations: generated data and OAuth sessions are in-memory, repository ingestion is not yet persisted, and GitHub OAuth credentials must be configured by the deployer.
